language: c

compiler:
  - gcc


integrations:
  notifications:
    - integrationName: email
      type: email
      on_success: never
      on_failure: change
      on_cancel: never
      on_pull_request: never

# pre_ci_boot:
#       image_name: brettbj/continuous_analysis_base
#       image_tag: latest
#       pull: true
#       options: "-e HOME=/root"


setup_ve:
  - gcc --version

build:
  cache: true
  cache_dir_list:
    - /root/miniconda/pkgs
    - /root/.conda/pkgs

  pre_ci_boot:
        image_name: jfaria/kima-docker
        image_tag: latest
        pull: true
        options: "-e HOME=/root"

  ci:
    - pwd 
    - echo $SHIPPABLE_BUILD_DIR
    - echo $HOME

    # install conda
    # - wget http://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh
    # - bash miniconda.sh -b -p $HOME/miniconda
    # - export PATH="$HOME/miniconda/bin:$PATH"
    # - conda config --set always_yes yes --set changeps1 no
    # - conda update -q conda
    - conda info -a
    # - conda create -q -n shippable-env python=3.6 scipy numpy matplotlib jupyter
    # - source activate shippable-env
    - which python
    - conda list

    # install kima and pykima
    # - git clone --recursive https://github.com/j-faria/kima.git
    # - cd kima
    # - make -j 2
    # - python setup.py install

    # # run kima on the examples
    # - cd examples/51Peg
    # - sed -i 's/5000/100/g' OPTIONS
    # - ./kima -t 2
    # # - ls
    # # - kima-showresults

    # # create the report
    # - jupyter nbconvert --to notebook ../../../report.ipynb --output-dir .
    # - jupyter nbconvert --to html --execute report.ipynb
    # - ls


    # # push it back to github
    # - cd $SHIPPABLE_BUILD_DIR
    # - mv kima/examples/51Peg/report.html reports

    # - git config user.email "joao.faria@astro.up.pt"
    # - git config user.name "João Faria"
    # - git config --global push.default simple
    # - git remote set-url origin https://github.com/j-faria/kima-them-all.git

    # # - git checkout master
    # # - git pull
    # - git add reports
    # - git commit -a -m "shippable output [ci skip]"
    # - git push

